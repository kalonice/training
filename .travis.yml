language: cpp python

python: '2.7'

sudo: false

compiler:
  - gcc

addons:
  apt:
    sources:
      - george-edison55-precise-backports
    packages:
      - cmake-data
      - cmake

before_script:
  - make cpplint

script:
  - make clean all
